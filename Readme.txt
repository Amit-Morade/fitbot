Create a virtual environment
- python -m venv venv
- source venv/bin/activate

Install everything as in the requirements.txt file
-pip install -r requirements.txt

Run the ./run_fitbot.sh script file and it will start all of the servers
